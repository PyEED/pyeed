FROM python:3.13-slim AS base

WORKDIR /app
ENV PYTHONUNBUFFERED=1

COPY pyproject.toml uv.lock* ./
RUN pip install --upgrade pip \
 && pip install "uv" \
 && uv sync --frozen --no-dev

COPY . .

EXPOSE 8080
CMD ["uv", "run", "uvicorn", "pyeed.api:app", "--host", "0.0.0.0", "--port", "8080"]
