---
icon: simple/modelcontextprotocol
---

# MCP Server

The MCP server for pyeed is a simple server that allows you to query the pyeed database using the MCP protocol.

## Usage

```bash
mcp-server start
```